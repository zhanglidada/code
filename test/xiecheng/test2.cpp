#include <iostream>
#include <vector>
using namespace std;

int func(vector<vector<int>> values) {
  int n = values.size();
  
}
int main() {
  vector<int> values;
  int tmp;
  cin >> tmp;
  while (tmp != (int)'#') {
    int a = tmp;
    values.push_back(a);
    cin >> tmp;
  }
}